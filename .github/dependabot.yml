# Simplified Dependabot - Quarterly updates only
version: 2

updates:
  # Root workspace - quarterly (first Monday of Jan, Apr, Jul, Oct)
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "quarterly"
    open-pull-requests-limit: 5
    groups:
      all-dependencies:
        patterns: ["*"]
        update-types: ["minor", "patch"]
    ignore:
      # Never auto-update major versions
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
    labels: ["dependencies", "automated"]
    commit-message:
      prefix: "chore(deps)"

  # GitHub Actions - quarterly
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "quarterly"
    open-pull-requests-limit: 3
    labels: ["dependencies", "ci"]
    commit-message:
      prefix: "ci(deps)"
